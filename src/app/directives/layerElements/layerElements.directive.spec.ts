import { LayersElementsDirective } from './layerElements.directive';

describe('LayersElementsDirective', () => {
	it('should create an instance', () => {
		const directive = new LayersElementsDirective(null, null);
		expect(directive).toBeTruthy();
	});
});

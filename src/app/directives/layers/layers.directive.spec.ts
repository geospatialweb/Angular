import { LayersDirective } from './layers.directive';

describe('LayersDirective', () => {
	it('should create an instance', () => {
		const directive = new LayersDirective(null, null);
		expect(directive).toBeTruthy();
	});
});
